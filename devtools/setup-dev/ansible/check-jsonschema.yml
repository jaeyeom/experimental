---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: uv.yml
  when: ansible_facts['os_family'] != "Darwin"

- name: Ensure check-jsonschema is present
  hosts: all
  tasks:
    - name: Include guard for check-jsonschema playbook
      block:
        - name: Stop early if the check-jsonschema playbook is already included
          meta: end_play
          when: check_jsonschema_playbook_imported is defined
        - name: Ensure the check-jsonschema playbook is not included
          set_fact:
            check_jsonschema_playbook_imported: true
          when: check_jsonschema_playbook_imported is not defined

    - name: Ensure check-jsonschema is present on MacOS
      block:
        - name: Check if check-jsonschema is installed
          shell: command -v check-jsonschema
          changed_when: False
      rescue:
        - name: Install check-jsonschema on MacOS
          community.general.homebrew:
            name: check-jsonschema
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure check-jsonschema is present on Termux
      block:
        - name: Ensure check-jsonschema is installed with uv
          shell: uv tool install check-jsonschema
          args:
            creates: ~/.local/bin/check-jsonschema
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure check-jsonschema is installed with uv
      shell: uv tool install check-jsonschema
      args:
        creates: ~/.local/bin/check-jsonschema
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
