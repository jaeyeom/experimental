---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure cargo is present
  hosts: all
  tasks:
    - name: Include guard for cargo playbook
      block:
        - name: Stop early if the cargo playbook is already included
          meta: end_play
          when: cargo_playbook_imported is defined
        - name: Ensure the cargo playbook is not included
          set_fact:
            cargo_playbook_imported: true
          when: cargo_playbook_imported is not defined

    - name: Ensure cargo is present on Termux
      block:
        - name: Check if cargo is installed
          shell: command -v cargo
          changed_when: False
      rescue:
        - name: Install cargo on Termux
          command: pkg install -y rust
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
