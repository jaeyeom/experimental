---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure direnv is present
  hosts: all
  tasks:
    - name: Include guard for direnv playbook
      block:
        - name: Stop early if the direnv playbook is already included
          meta: end_play
          when: direnv_playbook_imported is defined
        - name: Ensure the direnv playbook is not included
          set_fact:
            direnv_playbook_imported: true
          when: direnv_playbook_imported is not defined

    - name: Ensure direnv is present on MacOS
      block:
        - name: Check if direnv is installed
          shell: command -v direnv
          changed_when: False
      rescue:
        - name: Install direnv on MacOS
          community.general.homebrew:
            name: direnv
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure direnv is present on non-Termux, non-MacOS systems
      block:
        - name: Check if direnv is installed
          shell: command -v direnv
          changed_when: False
      rescue:
        - name: Install direnv on non-Termux, non-MacOS systems
          package:
            name: direnv
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure direnv is present on Termux
      block:
        - name: Check if direnv is installed
          shell: command -v direnv
          changed_when: False
      rescue:
        - name: Install direnv on Termux
          command: pkg install -y direnv
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
