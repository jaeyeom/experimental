---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-npm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined
- import_playbook: setup-cargo.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined
- import_playbook: setup-git.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined
- import_playbook: make.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined
- import_playbook: setup-user-bin-directory.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined

- name: Ensure codex is present
  hosts: all
  tasks:
    - name: Include guard for codex playbook
      block:
        - name: Stop early if the codex playbook is already included
          meta: end_play
          when: codex_playbook_imported is defined
        - name: Ensure the codex playbook is not included
          set_fact:
            codex_playbook_imported: true
          when: codex_playbook_imported is not defined

    - name: Ensure codex is present on MacOS
      block:
        - name: Check if codex is installed
          shell: command -v codex
          register: codex_installed
          ignore_errors: yes
          changed_when: False

        - name: Install codex using npm
          command: npm install -g @openai/codex
          when: codex_installed.rc != 0
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure codex is present on Termux
      block:
        - name: Check if codex is installed
          shell: command -v codex
          register: codex_installed
          failed_when: false
          changed_when: False

        - name: Install codex
          shell: |
            # Dependencies (rust, git, make) are ensured by imported playbooks

            # Clone and build
            # Verified repository: https://github.com/openai/codex (contains codex-rs)
            git clone https://github.com/openai/codex $TMPDIR/codex
            cd $TMPDIR/codex/codex-rs
            cargo build --release
            cp target/release/codex {{ user_bin_directory }}/
            rm -rf $TMPDIR/codex
          when: codex_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install codex via npm on Debian/Ubuntu
      block:
        - name: Check if codex is installed
          shell: command -v codex
          register: codex_installed
          ignore_errors: yes
          changed_when: False

        - name: Install codex using npm
          command: npm install -g @openai/codex
          when: codex_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
