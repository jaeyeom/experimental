---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: setup-cargo.yml

- name: Ensure rust-analyzer is present
  hosts: all
  tasks:
    - name: Include guard for rust-analyzer playbook
      block:
        - name: Stop early if the rust-analyzer playbook is already included
          meta: end_play
          when: rust_analyzer_playbook_imported is defined
        - name: Ensure the rust-analyzer playbook is not included
          set_fact:
            rust_analyzer_playbook_imported: true
          when: rust_analyzer_playbook_imported is not defined

    - name: Ensure rust-analyzer is present on MacOS
      block:
        - name: Check if rust-analyzer is installed
          shell: command -v rust-analyzer
          changed_when: False
      rescue:
        - name: Install rust-analyzer on MacOS
          community.general.homebrew:
            name: rust-analyzer
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure rust-analyzer is present on Termux
      block:
        - name: Check if rust-analyzer is installed
          shell: command -v rust-analyzer
          changed_when: False
      rescue:
        - name: Install rust-analyzer on Termux
          command: pkg install -y rust-analyzer
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install rust-analyzer via rustup-component on Debian/Ubuntu
      block:
        - name: Check if rust-analyzer is installed
          shell: command -v rust-analyzer
          register: rust_analyzer_installed
          ignore_errors: yes
          changed_when: False
          environment:
            PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"

        - name: Install rust-analyzer component via rustup
          command: rustup component add rust-analyzer
          when: rust_analyzer_installed.rc != 0
          environment:
            PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
