---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure antigravity is present
  hosts: all
  tasks:
    - name: Include guard for antigravity playbook
      block:
        - name: Stop early if the antigravity playbook is already included
          meta: end_play
          when: antigravity_playbook_imported is defined
        - name: Ensure the antigravity playbook is not included
          set_fact:
            antigravity_playbook_imported: true
          when: antigravity_playbook_imported is not defined

    - name: Ensure antigravity is present on MacOS
      block:
        - name: Check if antigravity is installed
          shell: command -v antigravity
          changed_when: False
      rescue:
        - name: Install antigravity on MacOS via cask
          community.general.homebrew_cask:
            name: antigravity
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Check if GPG key for antigravity exists
      ansible.builtin.stat:
        path: /etc/apt/keyrings/antigravity-repo-key.gpg
      register: antigravity_gpg_key
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Download GPG key for antigravity
      ansible.builtin.get_url:
        url: https://us-central1-apt.pkg.dev/doc/repo-signing-key.gpg
        dest: /tmp/antigravity-repo.gpg
        mode: '0644'
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not antigravity_gpg_key.stat.exists

    - name: Dearmor and install GPG key for antigravity
      ansible.builtin.shell: |
        cat /tmp/antigravity-repo.gpg | gpg --dearmor -o /etc/apt/keyrings/antigravity-repo-key.gpg
      args:
        creates: /etc/apt/keyrings/antigravity-repo-key.gpg
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not antigravity_gpg_key.stat.exists

    - name: Add apt repository for antigravity
      ansible.builtin.apt_repository:
        repo: "deb [signed-by=/etc/apt/keyrings/antigravity-repo-key.gpg] https://us-central1-apt.pkg.dev/projects/antigravity-auto-updater-dev/ antigravity-debian main"
        state: present
        filename: antigravity
        update_cache: yes
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"



    - name: Install antigravity via apt-repo on Debian/Ubuntu
      block:
        - name: Check if antigravity is installed
          shell: command -v antigravity
          changed_when: False
      rescue:
        - name: Install antigravity via apt
          ansible.builtin.apt:
            name: antigravity
            state: present
            update_cache: yes
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
