---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-nvm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

- name: Ensure gws is present
  hosts: all
  tasks:
    - name: Include guard for gws playbook
      block:
        - name: Stop early if the gws playbook is already included
          meta: end_play
          when: gws_playbook_imported is defined
        - name: Ensure the gws playbook is not included
          set_fact:
            gws_playbook_imported: true
          when: gws_playbook_imported is not defined

    - name: Ensure gws is present on MacOS
      block:
        - name: Check if gws is installed
          shell: command -v gws
          changed_when: False
      rescue:
        - name: Install gws on MacOS
          community.general.homebrew:
            name: googleworkspace-cli
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install gws via nvm on Debian/Ubuntu
      block:
        - name: Check if gws is installed
          shell: command -v gws
          register: gws_installed
          ignore_errors: yes
          changed_when: False

        - name: Install gws using npm through nvm
          shell: |
            export NVM_DIR="{{ detected_nvm_dir }}"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && nvm exec default npm install -g @googleworkspace/cli
          when: gws_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
