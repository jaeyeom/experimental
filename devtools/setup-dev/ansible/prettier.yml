---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-npm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined
- import_playbook: setup-nvm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

- name: Ensure prettier is present
  hosts: all
  tasks:
    - name: Include guard for prettier playbook
      block:
        - name: Stop early if the prettier playbook is already included
          meta: end_play
          when: prettier_playbook_imported is defined
        - name: Ensure the prettier playbook is not included
          set_fact:
            prettier_playbook_imported: true
          when: prettier_playbook_imported is not defined

    - name: Ensure prettier is present on MacOS
      block:
        - name: Check if prettier is installed
          shell: command -v prettier
          changed_when: False
      rescue:
        - name: Install prettier on MacOS
          community.general.homebrew:
            name: prettier
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure prettier is present on Termux
      block:
        - name: Check if prettier is installed
          shell: command -v prettier
          register: prettier_installed
          ignore_errors: yes
          changed_when: False

        - name: Install prettier using npm
          command: npm install -g prettier
          when: prettier_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install prettier via nvm on Debian/Ubuntu
      block:
        - name: Check if prettier is installed
          shell: command -v prettier
          register: prettier_installed
          ignore_errors: yes
          changed_when: False

        - name: Install prettier using npm through nvm
          command: nvm exec default npm install -g prettier
          when: prettier_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
