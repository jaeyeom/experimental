---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: setup-npm.yml
- import_playbook: setup-nvm.yml

- name: Ensure stylelint is present
  hosts: all
  tasks:
    - name: Include guard for stylelint playbook
      block:
        - name: Stop early if the stylelint playbook is already included
          meta: end_play
          when: stylelint_playbook_imported is defined
        - name: Ensure the stylelint playbook is not included
          set_fact:
            stylelint_playbook_imported: true
          when: stylelint_playbook_imported is not defined

    - name: Ensure stylelint is present on MacOS
      block:
        - name: Check if stylelint is installed
          shell: command -v stylelint
          changed_when: False
      rescue:
        - name: Install stylelint on MacOS
          community.general.homebrew:
            name: stylelint
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure stylelint is present on Termux
      block:
        - name: Check if stylelint is installed
          shell: command -v stylelint
          register: stylelint_installed
          ignore_errors: yes
          changed_when: False

        - name: Install stylelint using npm
          command: npm install -g stylelint
          when: stylelint_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install stylelint via nvm on Debian/Ubuntu
      block:
        - name: Check if stylelint is installed
          shell: command -v stylelint
          register: stylelint_installed
          ignore_errors: yes
          changed_when: False

        - name: Install stylelint using npm through nvm
          command: nvm exec default npm install -g stylelint
          when: stylelint_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
