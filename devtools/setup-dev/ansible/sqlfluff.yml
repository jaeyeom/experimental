---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: uv.yml

- name: Ensure sqlfluff is present
  hosts: all
  tasks:
    - name: Include guard for sqlfluff playbook
      block:
        - name: Stop early if the sqlfluff playbook is already included
          meta: end_play
          when: sqlfluff_playbook_imported is defined
        - name: Ensure the sqlfluff playbook is not included
          set_fact:
            sqlfluff_playbook_imported: true
          when: sqlfluff_playbook_imported is not defined

    - name: Ensure sqlfluff is present on MacOS
      block:
        - name: Check if sqlfluff is installed
          shell: command -v sqlfluff
          changed_when: False
      rescue:
        - name: Install sqlfluff on MacOS
          community.general.homebrew:
            name: sqlfluff
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure sqlfluff is present on Termux
      block:
        - name: Ensure if sqlfluff is installed
          ansible.builtin.pip:
            name: sqlfluff
            state: latest
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure sqlfluff is installed with uv
      shell: uv tool install sqlfluff
      args:
        creates: ~/.local/bin/sqlfluff
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
