---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-user-go-bin-directory.yml
  when: (ansible_facts['env']['TERMUX_VERSION'] is defined) or (ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin")

- name: Ensure goreleaser is present
  hosts: all
  tasks:
    - name: Include guard for goreleaser playbook
      block:
        - name: Stop early if the goreleaser playbook is already included
          meta: end_play
          when: goreleaser_playbook_imported is defined
        - name: Ensure the goreleaser playbook is not included
          set_fact:
            goreleaser_playbook_imported: true
          when: goreleaser_playbook_imported is not defined

    - name: Ensure goreleaser is present on MacOS
      block:
        - name: Check if goreleaser is installed
          shell: command -v goreleaser
          changed_when: False
      rescue:
        - name: Install goreleaser on MacOS
          community.general.homebrew:
            name: goreleaser
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure goreleaser is present on Termux
      block:
        - name: Check if goreleaser is installed
          shell: go version -m $(command -v goreleaser) | grep '^\s*mod\s'
          register: goreleaser_installed
          ignore_errors: yes
          changed_when: False

        - name: Extract goreleaser version
          block:
            - name: Set goreleaser facts
              set_fact:
                goreleaser_module_path: "{{ goreleaser_installed.stdout.split()[1] }}"
                goreleaser_module_version: "{{ goreleaser_installed.stdout.split()[2] }}"
            - name: Determine the latest goreleaser version
              command: go list -m -f "{{ '{{' }}.Version {{ '}}' }}" "{{ goreleaser_module_path }}@latest"
              register: goreleaser_latest
              ignore_errors: yes
              changed_when: False

            - name: Debug module path and version
              debug:
                msg: "{{ goreleaser_module_path }} {{ goreleaser_module_version }} => {{ goreleaser_latest.stdout }}"
          rescue:
            - name: Clear goreleaser facts
              set_fact:
                goreleaser_module_path: ""
                goreleaser_module_version: ""

        - name: Upgrade goreleaser
          command: go install github.com/goreleaser/goreleaser/v2@latest
          register: goreleaser_upgrade
          until: goreleaser_upgrade is succeeded
          retries: 3
          delay: 30
          when: goreleaser_module_version is not defined or goreleaser_module_version == "" or goreleaser_module_version != goreleaser_latest.stdout
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install goreleaser via go on Debian/Ubuntu
      block:
        - name: Check if goreleaser is installed
          shell: go version -m $(command -v goreleaser) | grep '^\s*mod\s'
          register: goreleaser_installed
          ignore_errors: yes
          changed_when: False

        - name: Extract goreleaser version
          block:
            - name: Set goreleaser facts
              set_fact:
                goreleaser_module_path: "{{ goreleaser_installed.stdout.split()[1] }}"
                goreleaser_module_version: "{{ goreleaser_installed.stdout.split()[2] }}"
            - name: Determine the latest goreleaser version
              command: go list -m -f "{{ '{{' }}.Version {{ '}}' }}" "{{ goreleaser_module_path }}@latest"
              register: goreleaser_latest
              ignore_errors: yes
              changed_when: False

            - name: Debug module path and version
              debug:
                msg: "{{ goreleaser_module_path }} {{ goreleaser_module_version }} => {{ goreleaser_latest.stdout }}"
          rescue:
            - name: Clear goreleaser facts
              set_fact:
                goreleaser_module_path: ""
                goreleaser_module_version: ""

        - name: Upgrade goreleaser
          command: go install github.com/goreleaser/goreleaser/v2@latest
          register: goreleaser_upgrade
          until: goreleaser_upgrade is succeeded
          retries: 3
          delay: 30
          when: goreleaser_module_version is not defined or goreleaser_module_version == "" or goreleaser_module_version != goreleaser_latest.stdout
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
