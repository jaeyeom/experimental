---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure mediainfo is present
  hosts: all
  tasks:
    - name: Include guard for mediainfo playbook
      block:
        - name: Stop early if the mediainfo playbook is already included
          meta: end_play
          when: mediainfo_playbook_imported is defined
        - name: Ensure the mediainfo playbook is not included
          set_fact:
            mediainfo_playbook_imported: true
          when: mediainfo_playbook_imported is not defined

    - name: Ensure mediainfo is present on MacOS
      block:
        - name: Check if mediainfo is installed
          shell: command -v mediainfo
          changed_when: False
      rescue:
        - name: Install mediainfo on MacOS
          community.general.homebrew:
            name: mediainfo
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure mediainfo is present on non-Termux, non-MacOS systems
      block:
        - name: Check if mediainfo is installed
          shell: command -v mediainfo
          changed_when: False
      rescue:
        - name: Install mediainfo on non-Termux, non-MacOS systems
          package:
            name: mediainfo
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure mediainfo is present on Termux
      block:
        - name: Check if mediainfo is installed
          shell: command -v mediainfo
          changed_when: False
      rescue:
        - name: Install mediainfo on Termux
          command: pkg install -y mediainfo
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
