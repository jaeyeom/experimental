---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure libatomic1 is present
  hosts: all
  tasks:
    - name: Include guard for libatomic1 playbook
      block:
        - name: Stop early if the libatomic1 playbook is already included
          meta: end_play
          when: libatomic1_playbook_imported is defined
        - name: Ensure the libatomic1 playbook is not included
          set_fact:
            libatomic1_playbook_imported: true
          when: libatomic1_playbook_imported is not defined

    - name: Ensure libatomic1 is present on MacOS
      block:
        - name: Check if libatomic1 is installed
          shell: ldconfig -p | grep -q libatomic
          changed_when: False
      rescue:
        - name: Install libatomic1 on MacOS
          community.general.homebrew:
            name: libatomic1
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure libatomic1 is present on non-Termux, non-MacOS systems
      block:
        - name: Check if libatomic1 is installed
          shell: ldconfig -p | grep -q libatomic
          changed_when: False
      rescue:
        - name: Install libatomic1 on non-Termux, non-MacOS systems
          package:
            name: libatomic1
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure libatomic1 is present on Termux
      block:
        - name: Check if libatomic1 is installed
          shell: ldconfig -p | grep -q libatomic
          changed_when: False
      rescue:
        - name: Install libatomic1 on Termux
          command: pkg install -y libatomic1
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
