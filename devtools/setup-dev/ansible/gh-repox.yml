---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gh.yml

- name: Ensure gh-repox is present
  hosts: all
  tasks:
    - name: Include guard for gh-repox playbook
      block:
        - name: Stop early if the gh-repox playbook is already included
          meta: end_play
          when: gh_repox_playbook_imported is defined
        - name: Ensure the gh-repox playbook is not included
          set_fact:
            gh_repox_playbook_imported: true
          when: gh_repox_playbook_imported is not defined

    - name: Check if gh-repox extension is installed
      shell: gh extension list | grep -q 'gh-repox'
      register: gh_repox_installed
      failed_when: false
      changed_when: False

    - name: Install gh-repox extension
      command: gh extension install jaeyeom/gh-repox
      when: gh_repox_installed.rc != 0

    - name: Upgrade gh-repox extension
      command: gh extension upgrade gh-repox
      when: gh_repox_installed.rc == 0
