---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gh.yml
- import_playbook: setup-user-go-bin-directory.yml

- name: Ensure act is present
  hosts: all
  tasks:
    - name: Include guard for act playbook
      block:
        - name: Stop early if the act playbook is already included
          meta: end_play
          when: act_playbook_imported is defined
        - name: Ensure the act playbook is not included
          set_fact:
            act_playbook_imported: true
          when: act_playbook_imported is not defined

    - name: Ensure act is present on MacOS
      block:
        - name: Check if act is installed
          shell: command -v act
          changed_when: False
      rescue:
        - name: Install act on MacOS
          community.general.homebrew:
            name: act
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure act is present on Termux
      block:
        - name: Check if act is installed
          shell: go version -m $(command -v act) | grep '^\s*mod\s'
          register: act_installed
          ignore_errors: yes
          changed_when: False

        - name: Extract act version
          block:
            - name: Set act facts
              set_fact:
                act_module_path: "{{ act_installed.stdout.split()[1] }}"
                act_module_version: "{{ act_installed.stdout.split()[2] }}"
            - name: Determine the latest act version
              command: go list -m -f "{{ '{{' }}.Version {{ '}}' }}" "{{ act_module_path }}@latest"
              register: act_latest
              ignore_errors: yes
              changed_when: False

            - name: Debug module path and version
              debug:
                msg: "{{ act_module_path }} {{ act_module_version }} => {{ act_latest.stdout }}"
          rescue:
            - name: Clear act facts
              set_fact:
                act_module_path: ""
                act_module_version: ""

        - name: Upgrade act
          command: go install github.com/nektos/act@latest
          when: act_module_version is not defined or act_module_version == "" or act_module_version != act_latest.stdout
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install act via go on Debian/Ubuntu
      block:
        - name: Check if act is installed
          shell: go version -m $(command -v act) | grep '^\s*mod\s'
          register: act_installed
          ignore_errors: yes
          changed_when: False

        - name: Extract act version
          block:
            - name: Set act facts
              set_fact:
                act_module_path: "{{ act_installed.stdout.split()[1] }}"
                act_module_version: "{{ act_installed.stdout.split()[2] }}"
            - name: Determine the latest act version
              command: go list -m -f "{{ '{{' }}.Version {{ '}}' }}" "{{ act_module_path }}@latest"
              register: act_latest
              ignore_errors: yes
              changed_when: False

            - name: Debug module path and version
              debug:
                msg: "{{ act_module_path }} {{ act_module_version }} => {{ act_latest.stdout }}"
          rescue:
            - name: Clear act facts
              set_fact:
                act_module_path: ""
                act_module_version: ""

        - name: Upgrade act
          command: go install github.com/nektos/act@latest
          when: act_module_version is not defined or act_module_version == "" or act_module_version != act_latest.stdout
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
