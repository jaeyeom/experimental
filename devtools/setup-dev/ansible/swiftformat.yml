---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure swiftformat is present
  hosts: all
  tasks:
    - name: Include guard for swiftformat playbook
      block:
        - name: Stop early if the swiftformat playbook is already included
          meta: end_play
          when: swiftformat_playbook_imported is defined
        - name: Ensure the swiftformat playbook is not included
          set_fact:
            swiftformat_playbook_imported: true
          when: swiftformat_playbook_imported is not defined

    - name: Ensure swiftformat is present on MacOS
      block:
        - name: Check if swiftformat is installed
          shell: command -v swiftformat
          changed_when: False
      rescue:
        - name: Install swiftformat on MacOS
          community.general.homebrew:
            name: swiftformat
            state: present
      when: ansible_facts['os_family'] == "Darwin"
