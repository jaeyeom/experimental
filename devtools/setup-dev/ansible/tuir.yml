---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: uv.yml
  when: (ansible_facts['os_family'] == "Darwin") or (ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin")

- name: Ensure tuir is present
  hosts: all
  tasks:
    - name: Include guard for tuir playbook
      block:
        - name: Stop early if the tuir playbook is already included
          meta: end_play
          when: tuir_playbook_imported is defined
        - name: Ensure the tuir playbook is not included
          set_fact:
            tuir_playbook_imported: true
          when: tuir_playbook_imported is not defined

    - name: Ensure tuir is present on MacOS
      block:
        - name: Ensure tuir is installed with uv
          shell: uv tool install tuir-continued
          args:
            creates: ~/.local/bin/tuir
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure tuir is present on Termux
      block:
        - name: Ensure if tuir is installed
          ansible.builtin.pip:
            name: tuir-continued
            state: latest
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure tuir is installed with uv
      shell: uv tool install tuir-continued
      args:
        creates: ~/.local/bin/tuir
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
