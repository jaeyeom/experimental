---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure wget is present
  hosts: all
  tasks:
    - name: Include guard for wget playbook
      block:
        - name: Stop early if the wget playbook is already included
          meta: end_play
          when: wget_playbook_imported is defined
        - name: Ensure the wget playbook is not included
          set_fact:
            wget_playbook_imported: true
          when: wget_playbook_imported is not defined

    - name: Ensure wget is present on MacOS
      block:
        - name: Check if wget is installed
          shell: command -v wget
          changed_when: False
      rescue:
        - name: Install wget on MacOS
          community.general.homebrew:
            name: wget
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure wget is present on non-Termux, non-MacOS systems
      block:
        - name: Check if wget is installed
          shell: command -v wget
          changed_when: False
      rescue:
        - name: Install wget on non-Termux, non-MacOS systems
          package:
            name: wget
            state: present
          become: yes
      when: ansible_env.TERMUX_VERSION is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure wget is present on Termux
      block:
        - name: Check if wget is installed
          shell: command -v wget
          changed_when: False
      rescue:
        - name: Install wget on Termux
          command: pkg install -y wget
      when: ansible_env.TERMUX_VERSION is defined
