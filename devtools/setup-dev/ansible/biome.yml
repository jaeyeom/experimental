---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-npm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined
- import_playbook: setup-nvm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

- name: Ensure biome is present
  hosts: all
  tasks:
    - name: Include guard for biome playbook
      block:
        - name: Stop early if the biome playbook is already included
          meta: end_play
          when: biome_playbook_imported is defined
        - name: Ensure the biome playbook is not included
          set_fact:
            biome_playbook_imported: true
          when: biome_playbook_imported is not defined

    - name: Ensure biome is present on MacOS
      block:
        - name: Check if biome is installed
          shell: command -v biome
          changed_when: False
      rescue:
        - name: Install biome on MacOS
          community.general.homebrew:
            name: biome
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure biome is present on Termux
      block:
        - name: Check if biome is installed
          shell: command -v biome
          register: biome_installed
          ignore_errors: yes
          changed_when: False

        - name: Install biome using npm
          command: npm install -g @biomejs/biome
          when: biome_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install biome via nvm on Debian/Ubuntu
      block:
        - name: Check if biome is installed
          shell: command -v biome
          register: biome_installed
          ignore_errors: yes
          changed_when: False

        - name: Install biome using npm through nvm
          command: nvm exec default npm install -g @biomejs/biome
          when: biome_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
