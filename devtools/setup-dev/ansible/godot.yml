---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure godot is present
  hosts: all
  tasks:
    - name: Include guard for godot playbook
      block:
        - name: Stop early if the godot playbook is already included
          meta: end_play
          when: godot_playbook_imported is defined
        - name: Ensure the godot playbook is not included
          set_fact:
            godot_playbook_imported: true
          when: godot_playbook_imported is not defined

    - name: Ensure godot is present on MacOS
      block:
        - name: Check if godot is installed
          shell: command -v godot
          changed_when: False
      rescue:
        - name: Install godot on MacOS via cask
          community.general.homebrew_cask:
            name: godot
            state: present
      when: ansible_facts['os_family'] == "Darwin"
