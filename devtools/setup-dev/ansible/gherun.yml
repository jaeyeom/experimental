---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gh.yml
- import_playbook: setup-user-go-bin-directory.yml

- name: Ensure gherun is present
  hosts: all
  tasks:
    - name: Include guard for gherun playbook
      block:
        - name: Stop early if the gherun playbook is already included
          meta: end_play
          when: gherun_playbook_imported is defined
        - name: Ensure the gherun playbook is not included
          set_fact:
            gherun_playbook_imported: true
          when: gherun_playbook_imported is not defined

    - name: Check if gherun is installed
      shell: go version -m $(command -v gherun) | grep '^\s*mod\s'
      register: gherun_installed
      ignore_errors: yes
      changed_when: False

    - name: Extract gherun version
      block:
        - name: Set gherun facts
          set_fact:
            gherun_module_path: "{{ gherun_installed.stdout.split()[1] }}"
            gherun_module_version: "{{ gherun_installed.stdout.split()[2] }}"
        - name: Determine the latest gherun version
          command: go list -m -f "{{ '{{' }}.Version {{ '}}' }}" "{{ gherun_module_path }}@latest"
          register: gherun_latest
          ignore_errors: yes
          changed_when: False

        - name: Debug module path and version
          debug:
            msg: "{{ gherun_module_path }} {{ gherun_module_version }} => {{ gherun_latest.stdout }}"
      rescue:
        - name: Clear gherun facts
          set_fact:
            gherun_module_path: ""
            gherun_module_version: ""

    - name: Upgrade gherun
      command: go install github.com/jaeyeom/experimental/devtools/gherun/cmd/gherun@latest
      when: gherun_module_version is not defined or gherun_module_version == "" or gherun_module_version != gherun_latest.stdout
