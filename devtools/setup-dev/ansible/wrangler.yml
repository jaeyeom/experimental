---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-npm.yml
  when: (ansible_facts['os_family'] == "Darwin") or (ansible_facts['env']['TERMUX_VERSION'] is defined)
- import_playbook: setup-nvm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

- name: Ensure wrangler is present
  hosts: all
  tasks:
    - name: Include guard for wrangler playbook
      block:
        - name: Stop early if the wrangler playbook is already included
          meta: end_play
          when: wrangler_playbook_imported is defined
        - name: Ensure the wrangler playbook is not included
          set_fact:
            wrangler_playbook_imported: true
          when: wrangler_playbook_imported is not defined

    - name: Ensure wrangler is present on MacOS
      block:
        - name: Check if wrangler is installed
          shell: command -v wrangler
          register: wrangler_installed
          ignore_errors: yes
          changed_when: False

        - name: Install wrangler using npm
          command: npm install -g wrangler
          when: wrangler_installed.rc != 0
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure wrangler is present on Termux
      block:
        - name: Check if wrangler is installed
          shell: command -v wrangler
          register: wrangler_installed
          ignore_errors: yes
          changed_when: False

        - name: Install wrangler using npm
          command: npm install -g wrangler
          when: wrangler_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install wrangler via nvm on Debian/Ubuntu
      block:
        - name: Check if wrangler is installed
          shell: command -v wrangler
          register: wrangler_installed
          ignore_errors: yes
          changed_when: False

        - name: Install wrangler using npm through nvm
          shell: |
            export NVM_DIR="{{ detected_nvm_dir }}"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && nvm exec default npm install -g wrangler
          when: wrangler_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
