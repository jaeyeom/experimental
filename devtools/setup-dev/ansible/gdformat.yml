---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: uv.yml

- name: Ensure gdformat is present
  hosts: all
  tasks:
    - name: Include guard for gdformat playbook
      block:
        - name: Stop early if the gdformat playbook is already included
          meta: end_play
          when: gdformat_playbook_imported is defined
        - name: Ensure the gdformat playbook is not included
          set_fact:
            gdformat_playbook_imported: true
          when: gdformat_playbook_imported is not defined

    - name: Ensure gdformat is present on MacOS
      block:
        - name: Ensure gdformat is installed with uv
          shell: uv tool install gdtoolkit
          args:
            creates: ~/.local/bin/gdformat
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure gdformat is present on Termux
      block:
        - name: Ensure gdformat is installed with uv
          shell: uv tool install gdtoolkit
          args:
            creates: ~/.local/bin/gdformat
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure gdformat is installed with uv
      shell: uv tool install gdtoolkit
      args:
        creates: ~/.local/bin/gdformat
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
