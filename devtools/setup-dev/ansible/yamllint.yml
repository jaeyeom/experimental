---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure yamllint is present
  hosts: all
  tasks:
    - name: Include guard for yamllint playbook
      block:
        - name: Stop early if the yamllint playbook is already included
          meta: end_play
          when: yamllint_playbook_imported is defined
        - name: Ensure the yamllint playbook is not included
          set_fact:
            yamllint_playbook_imported: true
          when: yamllint_playbook_imported is not defined

    - name: Ensure yamllint is present on MacOS
      block:
        - name: Check if yamllint is installed
          shell: command -v yamllint
          changed_when: False
      rescue:
        - name: Install yamllint on MacOS
          community.general.homebrew:
            name: yamllint
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure yamllint is present on Termux
      block:
        - name: Ensure if yamllint is installed
          ansible.builtin.pip:
            name: yamllint
            state: latest
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install yamllint via package on Debian/Ubuntu
      block:
        - name: Install yamllint on non-Termux, non-MacOS systems
          package:
            name: yamllint
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
