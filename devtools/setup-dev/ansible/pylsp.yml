---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: uv.yml
  when: (ansible_facts['env']['TERMUX_VERSION'] is defined) or (ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin")

- name: Ensure pylsp is present
  hosts: all
  tasks:
    - name: Include guard for pylsp playbook
      block:
        - name: Stop early if the pylsp playbook is already included
          meta: end_play
          when: pylsp_playbook_imported is defined
        - name: Ensure the pylsp playbook is not included
          set_fact:
            pylsp_playbook_imported: true
          when: pylsp_playbook_imported is not defined

    - name: Ensure pylsp is present on MacOS
      block:
        - name: Check if pylsp is installed
          shell: command -v pylsp
          changed_when: False
      rescue:
        - name: Install pylsp on MacOS
          community.general.homebrew:
            name: python-lsp-server
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure pylsp is present on Termux
      block:
        - name: Ensure pylsp is installed with uv
          shell: uv tool install python-lsp-server
          args:
            creates: ~/.local/bin/pylsp
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure pylsp is installed with uv
      shell: uv tool install python-lsp-server
      args:
        creates: ~/.local/bin/pylsp
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
