---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure delta is present
  hosts: all
  tasks:
    - name: Include guard for delta playbook
      block:
        - name: Stop early if the delta playbook is already included
          meta: end_play
          when: delta_playbook_imported is defined
        - name: Ensure the delta playbook is not included
          set_fact:
            delta_playbook_imported: true
          when: delta_playbook_imported is not defined

    - name: Ensure delta is present on MacOS
      block:
        - name: Check if delta is installed
          shell: command -v delta
          changed_when: False
      rescue:
        - name: Install delta on MacOS
          community.general.homebrew:
            name: git-delta
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure delta is present on Termux
      block:
        - name: Check if delta is installed
          shell: command -v delta
          changed_when: False
      rescue:
        - name: Install delta on Termux
          command: pkg install -y git-delta
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure backports is added to sources.list.d
      ansible.builtin.apt_repository:
        repo: "deb http://deb.debian.org/debian {{ ansible_facts['distribution_release'] }}-backports main contrib non-free non-free-firmware"
        state: present
        update_cache: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Debian"
      become: yes
      ignore_errors: yes



    - name: Install delta via debian on Debian
      block:
        - name: Check if delta is installed
          shell: command -v delta
          changed_when: False
      rescue:
        - name: Install delta on non-Termux, non-MacOS systems
          package:
            name: git-delta
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Debian"

    - name: Install delta via ubuntu on Ubuntu
      block:
        - name: Check if delta is installed
          shell: command -v delta
          changed_when: False
      rescue:
        - name: Install delta on non-Termux, non-MacOS systems
          package:
            name: git-delta
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Ubuntu"
