---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure pkg-config is present
  hosts: all
  tasks:
    - name: Include guard for pkg-config playbook
      block:
        - name: Stop early if the pkg-config playbook is already included
          meta: end_play
          when: pkg_config_playbook_imported is defined
        - name: Ensure the pkg-config playbook is not included
          set_fact:
            pkg_config_playbook_imported: true
          when: pkg_config_playbook_imported is not defined

    - name: Ensure pkg-config is present on MacOS
      block:
        - name: Check if pkg-config is installed
          shell: command -v pkg-config
          changed_when: False
      rescue:
        - name: Install pkg-config on MacOS
          community.general.homebrew:
            name: pkg-config
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure pkg-config is present on non-Termux, non-MacOS systems
      block:
        - name: Check if pkg-config is installed
          shell: command -v pkg-config
          changed_when: False
      rescue:
        - name: Install pkg-config on non-Termux, non-MacOS systems
          package:
            name: pkg-config
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure pkg-config is present on Termux
      block:
        - name: Check if pkg-config is installed
          shell: command -v pkg-config
          changed_when: False
      rescue:
        - name: Install pkg-config on Termux
          command: pkg install -y pkg-config
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
