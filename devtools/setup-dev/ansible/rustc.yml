---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure rustc is present
  hosts: all
  tasks:
    - name: Include guard for rustc playbook
      block:
        - name: Stop early if the rustc playbook is already included
          meta: end_play
          when: rustc_playbook_imported is defined
        - name: Ensure the rustc playbook is not included
          set_fact:
            rustc_playbook_imported: true
          when: rustc_playbook_imported is not defined

    - name: Ensure rustc is present on MacOS
      block:
        - name: Check if rustc is installed
          shell: command -v rustc
          changed_when: False
      rescue:
        - name: Install rustc on MacOS
          community.general.homebrew:
            name: rust
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure rustc is present on Termux
      block:
        - name: Check if rustc is installed
          shell: command -v rustc
          changed_when: False
      rescue:
        - name: Install rustc on Termux
          command: pkg install -y rust
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install rustc via package on Debian/Ubuntu
      block:
        - name: Install rustc on non-Termux, non-MacOS systems
          package:
            name: rust-all
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
