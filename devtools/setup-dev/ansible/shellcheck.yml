---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure shellcheck is present
  hosts: all
  tasks:
    - name: Include guard for shellcheck playbook
      block:
        - name: Stop early if the shellcheck playbook is already included
          meta: end_play
          when: shellcheck_playbook_imported is defined
        - name: Ensure the shellcheck playbook is not included
          set_fact:
            shellcheck_playbook_imported: true
          when: shellcheck_playbook_imported is not defined

    - name: Ensure shellcheck is present on MacOS
      block:
        - name: Check if shellcheck is installed
          shell: command -v shellcheck
          changed_when: False
      rescue:
        - name: Install shellcheck on MacOS
          community.general.homebrew:
            name: shellcheck
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure shellcheck is present on non-Termux, non-MacOS systems
      block:
        - name: Check if shellcheck is installed
          shell: command -v shellcheck
          changed_when: False
      rescue:
        - name: Install shellcheck on non-Termux, non-MacOS systems
          package:
            name: shellcheck
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure shellcheck is present on Termux
      block:
        - name: Check if shellcheck is installed
          shell: command -v shellcheck
          changed_when: False
      rescue:
        - name: Install shellcheck on Termux
          command: pkg install -y shellcheck
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
