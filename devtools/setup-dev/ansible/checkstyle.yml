---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: setup-java.yml

- name: Ensure checkstyle is present
  hosts: all
  tasks:
    - name: Include guard for checkstyle playbook
      block:
        - name: Stop early if the checkstyle playbook is already included
          meta: end_play
          when: checkstyle_playbook_imported is defined
        - name: Ensure the checkstyle playbook is not included
          set_fact:
            checkstyle_playbook_imported: true
          when: checkstyle_playbook_imported is not defined

    - name: Ensure checkstyle is present on MacOS
      block:
        - name: Check if checkstyle is installed
          shell: command -v checkstyle
          changed_when: False
      rescue:
        - name: Install checkstyle on MacOS
          community.general.homebrew:
            name: checkstyle
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install checkstyle via package on Debian/Ubuntu
      block:
        - name: Check if checkstyle is installed
          shell: command -v checkstyle
          changed_when: False
      rescue:
        - name: Install checkstyle on non-Termux, non-MacOS systems
          package:
            name: checkstyle
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
