---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-npm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined
- import_playbook: setup-nvm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

- name: Ensure tsc is present
  hosts: all
  tasks:
    - name: Include guard for tsc playbook
      block:
        - name: Stop early if the tsc playbook is already included
          meta: end_play
          when: tsc_playbook_imported is defined
        - name: Ensure the tsc playbook is not included
          set_fact:
            tsc_playbook_imported: true
          when: tsc_playbook_imported is not defined

    - name: Ensure tsc is present on MacOS
      block:
        - name: Check if tsc is installed
          shell: command -v tsc
          changed_when: False
      rescue:
        - name: Install tsc on MacOS
          community.general.homebrew:
            name: typescript
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure tsc is present on Termux
      block:
        - name: Check if tsc is installed
          shell: command -v tsc
          register: tsc_installed
          ignore_errors: yes
          changed_when: False

        - name: Install tsc using npm
          command: npm install -g typescript
          when: tsc_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install tsc via nvm on Debian/Ubuntu
      block:
        - name: Check if tsc is installed
          shell: command -v tsc
          register: tsc_installed
          ignore_errors: yes
          changed_when: False

        - name: Install tsc using npm through nvm
          shell: |
            export NVM_DIR="{{ detected_nvm_dir }}"
            [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && nvm exec default npm install -g typescript
          when: tsc_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
