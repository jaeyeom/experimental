---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: curl.yml

- name: Ensure herdr is present
  hosts: all
  tasks:
    - name: Include guard for herdr playbook
      block:
        - name: Stop early if the herdr playbook is already included
          meta: end_play
          when: herdr_playbook_imported is defined
        - name: Ensure the herdr playbook is not included
          set_fact:
            herdr_playbook_imported: true
          when: herdr_playbook_imported is not defined

    - name: Ensure herdr is present on MacOS
      block:
        - name: Check if herdr is installed
          shell: command -v herdr
          changed_when: False
      rescue:
        - name: Install herdr on MacOS
          community.general.homebrew:
            name: herdr
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install herdr via shell on Debian/Ubuntu
      block:
        - name: Check if herdr is installed
          shell: command -v herdr
          register: herdr_command_check
          failed_when: false
          changed_when: False
          environment:
            PATH: "{{ ansible_facts['env']['HOME'] }}/.local/bin:{{ ansible_facts['env']['PATH'] }}"

        - name: Get installed herdr version
          command: herdr --version
          register: herdr_version_output
          failed_when: false
          changed_when: False
          when: herdr_command_check.rc == 0
          environment:
            PATH: "{{ ansible_facts['env']['HOME'] }}/.local/bin:{{ ansible_facts['env']['PATH'] }}"

        - name: Parse installed herdr version
          set_fact:
            herdr_installed_version: "{{ (herdr_version_output.stdout | regex_search('herdr ([0-9.]+)', '\\1')) | default(['0.0.0'], true) | first }}"
          when: herdr_command_check.rc == 0

        - name: Set default version when herdr is not installed
          set_fact:
            herdr_installed_version: "0.0.0"
          when: herdr_command_check.rc != 0

        - name: Get latest available herdr version from GitHub
          ansible.builtin.include_tasks: tasks/github-release-info.yml
          vars:
            github_release_name: herdr
            github_release_owner: herdrdev
            github_release_repo: herdr
            github_release_register: herdr_latest_release

        - name: Parse latest herdr version from GitHub response
          set_fact:
            herdr_latest_version: "{{ herdr_latest_release.json.tag_name | regex_replace('^v', '') }}"

        - name: Install/update herdr if outdated
          shell: |
            {{ playbook_dir }}/verified-run exec https://herdr.dev/install.sh
          when: herdr_installed_version != herdr_latest_version
          environment:
            PATH: "{{ ansible_facts['env']['HOME'] }}/.local/bin:{{ ansible_facts['env']['PATH'] }}"
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
