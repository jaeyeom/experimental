---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: setup-rust.yml

- name: Ensure rustfmt is present
  hosts: all
  tasks:
    - name: Include guard for rustfmt playbook
      block:
        - name: Stop early if the rustfmt playbook is already included
          meta: end_play
          when: rustfmt_playbook_imported is defined
        - name: Ensure the rustfmt playbook is not included
          set_fact:
            rustfmt_playbook_imported: true
          when: rustfmt_playbook_imported is not defined

    - name: Ensure rustfmt is present on MacOS
      block:
        - name: Check if rustfmt is installed
          shell: command -v rustfmt
          changed_when: False
      rescue:
        - name: Install rustfmt on MacOS
          community.general.homebrew:
            name: rustfmt
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure rustfmt is present on Termux
      block:
        - name: Check if rustfmt is installed
          shell: command -v rustfmt
          register: rustfmt_installed
          ignore_errors: yes
          changed_when: False

        - name: Install rustfmt component via rustup
          command: rustup component add rustfmt
          when: rustfmt_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install rustfmt via rustup-component on Debian/Ubuntu
      block:
        - name: Check if rustfmt is installed
          shell: command -v rustfmt
          register: rustfmt_installed
          ignore_errors: yes
          changed_when: False

        - name: Install rustfmt component via rustup
          command: rustup component add rustfmt
          when: rustfmt_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
