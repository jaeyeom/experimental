---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-npm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined
- import_playbook: setup-nvm.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

- name: Ensure eslint is present
  hosts: all
  tasks:
    - name: Include guard for eslint playbook
      block:
        - name: Stop early if the eslint playbook is already included
          meta: end_play
          when: eslint_playbook_imported is defined
        - name: Ensure the eslint playbook is not included
          set_fact:
            eslint_playbook_imported: true
          when: eslint_playbook_imported is not defined

    - name: Ensure eslint is present on MacOS
      block:
        - name: Check if eslint is installed
          shell: command -v eslint
          changed_when: False
      rescue:
        - name: Install eslint on MacOS
          community.general.homebrew:
            name: eslint
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure eslint is present on Termux
      block:
        - name: Check if eslint is installed
          shell: command -v eslint
          register: eslint_installed
          ignore_errors: yes
          changed_when: False

        - name: Install eslint using npm
          command: npm install -g eslint
          when: eslint_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install eslint via nvm on Debian/Ubuntu
      block:
        - name: Check if eslint is installed
          shell: command -v eslint
          register: eslint_installed
          ignore_errors: yes
          changed_when: False

        - name: Install eslint using npm through nvm
          command: nvm exec default npm install -g eslint
          when: eslint_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
