---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: curl.yml

- name: Ensure nix is present
  hosts: all
  tasks:
    - name: Include guard for nix playbook
      block:
        - name: Stop early if the nix playbook is already included
          meta: end_play
          when: nix_playbook_imported is defined
        - name: Ensure the nix playbook is not included
          set_fact:
            nix_playbook_imported: true
          when: nix_playbook_imported is not defined

    - name: Ensure nix is present on MacOS
      block:
        - name: Check if nix is installed
          shell: command -v nix
          register: nix_installed
          failed_when: false
          changed_when: False

        - name: Install nix
          shell: |
            if test -d /nix; then exit 0; fi
            curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix | sh -s -- install --no-confirm
          when: nix_installed.rc != 0
          become: yes
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install nix via shell on Debian/Ubuntu
      block:
        - name: Check if nix is installed
          shell: command -v nix
          register: nix_installed
          failed_when: false
          changed_when: False

        - name: Install nix
          shell: |
            if test -d /nix; then exit 0; fi
            curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix | sh -s -- install --no-confirm
          when: nix_installed.rc != 0
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure ~/.config/nix directory exists
      ansible.builtin.file:
        path: "{{ ansible_facts['env']['HOME'] }}/.config/nix"
        state: directory
        mode: '0755'
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined

    - name: Enable flakes and nix-command in user nix.conf
      ansible.builtin.lineinfile:
        path: "{{ ansible_facts['env']['HOME'] }}/.config/nix/nix.conf"
        line: "experimental-features = nix-command flakes"
        regexp: "^experimental-features"
        create: yes
        mode: '0644'
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined
