---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure terraform-ls is present
  hosts: all
  tasks:
    - name: Include guard for terraform-ls playbook
      block:
        - name: Stop early if the terraform-ls playbook is already included
          meta: end_play
          when: terraform_ls_playbook_imported is defined
        - name: Ensure the terraform-ls playbook is not included
          set_fact:
            terraform_ls_playbook_imported: true
          when: terraform_ls_playbook_imported is not defined

    - name: Tap hashicorp/tap for terraform-ls
      community.general.homebrew_tap:
        name: hashicorp/tap
        state: present
      when: ansible_facts['os_family'] == "Darwin"



    - name: Ensure terraform-ls is present on MacOS
      block:
        - name: Check if terraform-ls is installed
          shell: command -v terraform-ls
          changed_when: False
      rescue:
        - name: Install terraform-ls on MacOS
          community.general.homebrew:
            name: hashicorp/tap/terraform-ls
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Check if GPG key for terraform-ls exists
      ansible.builtin.stat:
        path: /usr/share/keyrings/hashicorp-archive-keyring.gpg
      register: terraform_ls_gpg_key
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Download GPG key for terraform-ls
      ansible.builtin.get_url:
        url: https://apt.releases.hashicorp.com/gpg
        dest: /tmp/terraform-ls-repo.gpg
        mode: '0644'
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not terraform_ls_gpg_key.stat.exists

    - name: Dearmor and install GPG key for terraform-ls
      ansible.builtin.shell: |
        cat /tmp/terraform-ls-repo.gpg | gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
      args:
        creates: /usr/share/keyrings/hashicorp-archive-keyring.gpg
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not terraform_ls_gpg_key.stat.exists

    - name: Add apt repository for terraform-ls
      ansible.builtin.deb822_repository:
        name: terraform-ls
        types: deb
        uris: "https://apt.releases.hashicorp.com"
        suites: "{{ ansible_facts['distribution_release'] }}"
        components:
          - main
        signed_by: /usr/share/keyrings/hashicorp-archive-keyring.gpg
        enabled: true
        state: present
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"



    - name: Install terraform-ls via apt-repo on Debian/Ubuntu
      block:
        - name: Check if terraform-ls is installed
          shell: command -v terraform-ls
          changed_when: False
      rescue:
        - name: Install terraform-ls via apt
          ansible.builtin.apt:
            name: terraform-ls
            state: present
            update_cache: yes
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
