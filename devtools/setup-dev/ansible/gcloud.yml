---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure gcloud is present
  hosts: all
  tasks:
    - name: Include guard for gcloud playbook
      block:
        - name: Stop early if the gcloud playbook is already included
          meta: end_play
          when: gcloud_playbook_imported is defined
        - name: Ensure the gcloud playbook is not included
          set_fact:
            gcloud_playbook_imported: true
          when: gcloud_playbook_imported is not defined

    - name: Ensure gcloud is present on MacOS
      block:
        - name: Check if gcloud is installed
          shell: command -v gcloud
          changed_when: False
      rescue:
        - name: Install gcloud on MacOS
          community.general.homebrew:
            name: google-cloud-sdk
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Check if GPG key for gcloud exists
      ansible.builtin.stat:
        path: /etc/apt/trusted.gpg.d/google-cloud.gpg
      register: gcloud_gpg_key
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Download GPG key for gcloud
      ansible.builtin.get_url:
        url: https://packages.cloud.google.com/apt/doc/apt-key.gpg
        dest: /tmp/gcloud-repo.gpg
        mode: '0644'
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not gcloud_gpg_key.stat.exists

    - name: Dearmor and install GPG key for gcloud
      ansible.builtin.shell: |
        cat /tmp/gcloud-repo.gpg | gpg --dearmor -o /etc/apt/trusted.gpg.d/google-cloud.gpg
      args:
        creates: /etc/apt/trusted.gpg.d/google-cloud.gpg
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not gcloud_gpg_key.stat.exists

    - name: Add apt repository for gcloud
      ansible.builtin.apt_repository:
        repo: "deb [signed-by=/etc/apt/trusted.gpg.d/google-cloud.gpg] https://packages.cloud.google.com/apt cloud-sdk main"
        state: present
        filename: gcloud
        update_cache: yes
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"



    - name: Install gcloud via apt-repo on Debian/Ubuntu
      block:
        - name: Check if gcloud is installed
          shell: command -v gcloud
          changed_when: False
      rescue:
        - name: Install gcloud via apt
          ansible.builtin.apt:
            name: google-cloud-cli
            state: present
            update_cache: yes
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
