---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure rustup is present
  hosts: all
  tasks:
    - name: Include guard for rustup playbook
      block:
        - name: Stop early if the rustup playbook is already included
          meta: end_play
          when: rustup_playbook_imported is defined
        - name: Ensure the rustup playbook is not included
          set_fact:
            rustup_playbook_imported: true
          when: rustup_playbook_imported is not defined

    - name: Ensure rustup is present on MacOS
      block:
        - name: Check if rustup is installed
          shell: command -v rustup
          changed_when: False
      rescue:
        - name: Install rustup on MacOS
          community.general.homebrew:
            name: rustup
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install rustup via package on Debian/Ubuntu
      block:
        - name: Install rustup on non-Termux, non-MacOS systems
          package:
            name: rustup
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
