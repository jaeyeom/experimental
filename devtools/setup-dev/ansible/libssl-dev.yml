---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure libssl-dev is present
  hosts: all
  tasks:
    - name: Include guard for libssl-dev playbook
      block:
        - name: Stop early if the libssl-dev playbook is already included
          meta: end_play
          when: libssl_dev_playbook_imported is defined
        - name: Ensure the libssl-dev playbook is not included
          set_fact:
            libssl_dev_playbook_imported: true
          when: libssl_dev_playbook_imported is not defined

    - name: Ensure libssl-dev is present on MacOS
      block:
        - name: Check if libssl-dev is installed
          shell: command -v libssl-dev
          changed_when: False
      rescue:
        - name: Install libssl-dev on MacOS
          community.general.homebrew:
            name: openssl
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure libssl-dev is present on non-Termux, non-MacOS systems
      block:
        - name: Check if libssl-dev is installed
          shell: command -v libssl-dev
          changed_when: False
      rescue:
        - name: Install libssl-dev on non-Termux, non-MacOS systems
          package:
            name: libssl-dev
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure libssl-dev is present on Termux
      block:
        - name: Check if libssl-dev is installed
          shell: command -v libssl-dev
          changed_when: False
      rescue:
        - name: Install libssl-dev on Termux
          command: pkg install -y openssl
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
