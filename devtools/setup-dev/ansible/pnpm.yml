---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: setup-npm.yml

- name: Ensure pnpm is present
  hosts: all
  tasks:
    - name: Include guard for pnpm playbook
      block:
        - name: Stop early if the pnpm playbook is already included
          meta: end_play
          when: pnpm_playbook_imported is defined
        - name: Ensure the pnpm playbook is not included
          set_fact:
            pnpm_playbook_imported: true
          when: pnpm_playbook_imported is not defined

    - name: Ensure pnpm is present on MacOS
      block:
        - name: Check if pnpm is installed
          shell: command -v pnpm
          changed_when: False
      rescue:
        - name: Install pnpm on MacOS
          community.general.homebrew:
            name: pnpm
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure pnpm is present on Termux
      block:
        - name: Check if pnpm is installed
          shell: command -v pnpm
          register: pnpm_installed
          ignore_errors: yes
          changed_when: False

        - name: Install pnpm using npm
          command: npm install -g pnpm
          when: pnpm_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install pnpm via npm on Debian/Ubuntu
      block:
        - name: Check if pnpm is installed
          shell: command -v pnpm
          register: pnpm_installed
          ignore_errors: yes
          changed_when: False

        - name: Install pnpm using npm
          command: npm install -g pnpm
          when: pnpm_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
