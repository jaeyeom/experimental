---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-cargo.yml
  when: (ansible_facts['env']['TERMUX_VERSION'] is defined) or (ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin")
- import_playbook: cargo-install-update.yml
  when: (ansible_facts['env']['TERMUX_VERSION'] is defined) or (ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin")

- name: Ensure viddy is present
  hosts: all
  tasks:
    - name: Include guard for viddy playbook
      block:
        - name: Stop early if the viddy playbook is already included
          meta: end_play
          when: viddy_playbook_imported is defined
        - name: Ensure the viddy playbook is not included
          set_fact:
            viddy_playbook_imported: true
          when: viddy_playbook_imported is not defined

    - name: Ensure viddy is present on MacOS
      block:
        - name: Check if viddy is installed
          shell: command -v viddy
          changed_when: False
      rescue:
        - name: Install viddy on MacOS
          community.general.homebrew:
            name: viddy
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure viddy is present on Termux
      block:
        - name: Check if viddy is installed
          shell: command -v viddy
          register: viddy_installed
          ignore_errors: yes
          changed_when: False
          environment:
            PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"

        - name: Install viddy using Cargo
          command: cargo install viddy
          when: viddy_installed.rc != 0
          vars:
            cargo_base_env:
              PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"
            cargo_termux_env:
              CARGO_BUILD_JOBS: "1"
          environment: "{{ cargo_base_env | combine(cargo_termux_env if ansible_facts['env']['TERMUX_VERSION'] is defined else {}) }}"

        - name: Update viddy to latest version
          command: cargo install-update viddy
          register: viddy_update_result
          changed_when: "viddy_update_result.stdout is search('Overall updated [1-9]')"
          when: viddy_installed.rc == 0
          vars:
            cargo_base_env:
              PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"
            cargo_termux_env:
              CARGO_BUILD_JOBS: "1"
          environment: "{{ cargo_base_env | combine(cargo_termux_env if ansible_facts['env']['TERMUX_VERSION'] is defined else {}) }}"
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install viddy via cargo on Debian/Ubuntu
      block:
        - name: Check if viddy is installed
          shell: command -v viddy
          register: viddy_installed
          ignore_errors: yes
          changed_when: False
          environment:
            PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"

        - name: Install viddy using Cargo
          command: cargo install viddy
          when: viddy_installed.rc != 0
          vars:
            cargo_base_env:
              PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"
            cargo_termux_env:
              CARGO_BUILD_JOBS: "1"
          environment: "{{ cargo_base_env | combine(cargo_termux_env if ansible_facts['env']['TERMUX_VERSION'] is defined else {}) }}"

        - name: Update viddy to latest version
          command: cargo install-update viddy
          register: viddy_update_result
          changed_when: "viddy_update_result.stdout is search('Overall updated [1-9]')"
          when: viddy_installed.rc == 0
          vars:
            cargo_base_env:
              PATH: "{{ ansible_facts['env']['HOME'] }}/.cargo/bin:{{ ansible_facts['env']['PATH'] }}"
            cargo_termux_env:
              CARGO_BUILD_JOBS: "1"
          environment: "{{ cargo_base_env | combine(cargo_termux_env if ansible_facts['env']['TERMUX_VERSION'] is defined else {}) }}"
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
