---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure ffmpegthumbnailer is present
  hosts: all
  tasks:
    - name: Include guard for ffmpegthumbnailer playbook
      block:
        - name: Stop early if the ffmpegthumbnailer playbook is already included
          meta: end_play
          when: ffmpegthumbnailer_playbook_imported is defined
        - name: Ensure the ffmpegthumbnailer playbook is not included
          set_fact:
            ffmpegthumbnailer_playbook_imported: true
          when: ffmpegthumbnailer_playbook_imported is not defined

    - name: Ensure ffmpegthumbnailer is present on MacOS
      block:
        - name: Check if ffmpegthumbnailer is installed
          shell: command -v ffmpegthumbnailer
          changed_when: False
      rescue:
        - name: Install ffmpegthumbnailer on MacOS
          community.general.homebrew:
            name: ffmpegthumbnailer
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure ffmpegthumbnailer is present on non-Termux, non-MacOS systems
      block:
        - name: Check if ffmpegthumbnailer is installed
          shell: command -v ffmpegthumbnailer
          changed_when: False
      rescue:
        - name: Install ffmpegthumbnailer on non-Termux, non-MacOS systems
          package:
            name: ffmpegthumbnailer
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure ffmpegthumbnailer is present on Termux
      block:
        - name: Check if ffmpegthumbnailer is installed
          shell: command -v ffmpegthumbnailer
          changed_when: False
      rescue:
        - name: Install ffmpegthumbnailer on Termux
          command: pkg install -y ffmpegthumbnailer
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
