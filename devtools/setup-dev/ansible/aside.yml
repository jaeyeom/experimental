---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: curl.yml
- import_playbook: setup-user-bin-directory.yml
- import_playbook: libatomic1.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

- name: Ensure aside is present
  hosts: all
  tasks:
    - name: Include guard for aside playbook
      block:
        - name: Stop early if the aside playbook is already included
          meta: end_play
          when: aside_playbook_imported is defined
        - name: Ensure the aside playbook is not included
          set_fact:
            aside_playbook_imported: true
          when: aside_playbook_imported is not defined

    - name: Ensure aside is present on MacOS
      block:
        - name: Check if aside is installed
          shell: command -v aside
          register: aside_installed
          failed_when: false
          changed_when: False
          environment:
            PATH: "{{ user_bin_directory }}:{{ ansible_facts['env']['PATH'] }}"

        - name: Install aside
          shell: |
            {{ playbook_dir }}/verified-run exec https://releases.aside.com/install.sh
          when: aside_installed.rc != 0
          environment:
            PATH: "{{ user_bin_directory }}:{{ ansible_facts['env']['PATH'] }}"
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install aside via shell on Debian/Ubuntu
      block:
        - name: Check if aside is installed
          shell: command -v aside
          register: aside_installed
          failed_when: false
          changed_when: False
          environment:
            PATH: "{{ user_bin_directory }}:{{ ansible_facts['env']['PATH'] }}"

        - name: Install aside
          shell: |
            {{ playbook_dir }}/verified-run exec https://releases.aside.com/install.sh
          when: aside_installed.rc != 0
          environment:
            PATH: "{{ user_bin_directory }}:{{ ansible_facts['env']['PATH'] }}"
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
