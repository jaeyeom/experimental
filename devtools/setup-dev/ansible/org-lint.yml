---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: emacs.yml
- import_playbook: setup-user-go-bin-directory.yml

- name: Ensure org-lint is present
  hosts: all
  tasks:
    - name: Include guard for org-lint playbook
      block:
        - name: Stop early if the org-lint playbook is already included
          meta: end_play
          when: org_lint_playbook_imported is defined
        - name: Ensure the org-lint playbook is not included
          set_fact:
            org_lint_playbook_imported: true
          when: org_lint_playbook_imported is not defined

    - name: Check if org-lint is installed
      shell: go version -m $(command -v org-lint) | grep '^\s*mod\s'
      register: org_lint_installed
      ignore_errors: yes
      changed_when: False

    - name: Extract org-lint version
      block:
        - name: Set org-lint facts
          set_fact:
            org_lint_module_path: "{{ org_lint_installed.stdout.split()[1] }}"
            org_lint_module_version: "{{ org_lint_installed.stdout.split()[2] }}"
        - name: Determine the latest org-lint version
          command: go list -m -f "{{ '{{' }}.Version {{ '}}' }}" "{{ org_lint_module_path }}@latest"
          register: org_lint_latest
          ignore_errors: yes
          changed_when: False

        - name: Debug module path and version
          debug:
            msg: "{{ org_lint_module_path }} {{ org_lint_module_version }} => {{ org_lint_latest.stdout }}"
      rescue:
        - name: Clear org-lint facts
          set_fact:
            org_lint_module_path: ""
            org_lint_module_version: ""

    - name: Upgrade org-lint
      command: go install github.com/jaeyeom/experimental/devtools/linters/cmd/org-lint@latest
      when: org_lint_module_version is not defined or org_lint_module_version == "" or org_lint_module_version != org_lint_latest.stdout
