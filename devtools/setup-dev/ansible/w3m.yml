---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure w3m is present
  hosts: all
  tasks:
    - name: Include guard for w3m playbook
      block:
        - name: Stop early if the w3m playbook is already included
          meta: end_play
          when: w3m_playbook_imported is defined
        - name: Ensure the w3m playbook is not included
          set_fact:
            w3m_playbook_imported: true
          when: w3m_playbook_imported is not defined

    - name: Ensure w3m is present on MacOS
      block:
        - name: Check if w3m is installed
          shell: command -v w3m
          changed_when: False
      rescue:
        - name: Install w3m on MacOS
          community.general.homebrew:
            name: w3m
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure w3m is present on non-Termux, non-MacOS systems
      block:
        - name: Check if w3m is installed
          shell: command -v w3m
          changed_when: False
      rescue:
        - name: Install w3m on non-Termux, non-MacOS systems
          package:
            name: w3m
            state: present
          become: yes
      when: ansible_env.TERMUX_VERSION is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure w3m is present on Termux
      block:
        - name: Check if w3m is installed
          shell: command -v w3m
          changed_when: False
      rescue:
        - name: Install w3m on Termux
          command: pkg install -y w3m
      when: ansible_env.TERMUX_VERSION is defined
