---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: git.yml
- import_playbook: curl.yml

- name: Ensure githooks is present
  hosts: all
  tasks:
    - name: Include guard for githooks playbook
      block:
        - name: Stop early if the githooks playbook is already included
          meta: end_play
          when: githooks_playbook_imported is defined
        - name: Ensure the githooks playbook is not included
          set_fact:
            githooks_playbook_imported: true
          when: githooks_playbook_imported is not defined

    - name: Ensure githooks is present
      block:
        - name: Check if githooks is installed
          shell: command -v githooks
          register: githooks_installed
          failed_when: false
          changed_when: False

        - name: Install githooks
          shell: |
            if git hooks --version >/dev/null 2>&1; then
                echo "Githooks already installed, skipping."
            else
                {{ playbook_dir }}/verified-run exec https://raw.githubusercontent.com/gabyx/githooks/main/scripts/install.sh -- -- --non-interactive
            fi
          when: githooks_installed.rc != 0
