---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure emacs is present
  hosts: all
  tasks:
    - name: Include guard for emacs playbook
      block:
        - name: Stop early if the emacs playbook is already included
          meta: end_play
          when: emacs_playbook_imported is defined
        - name: Ensure the emacs playbook is not included
          set_fact:
            emacs_playbook_imported: true
          when: emacs_playbook_imported is not defined

    - name: Tap d12frosted/emacs-plus for emacs
      community.general.homebrew_tap:
        name: d12frosted/emacs-plus
        state: present
      when: ansible_facts['os_family'] == "Darwin"



    - name: Ensure emacs is present on MacOS
      block:
        - name: Check if emacs is installed
          shell: command -v emacs
          changed_when: False
      rescue:
        - name: Install emacs on MacOS
          community.general.homebrew:
            name: emacs-plus
            state: present
            install_options:
              - with-dbus
              - with-imagemagick
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure emacs is present on Termux
      block:
        - name: Check if emacs is installed
          shell: command -v emacs
          changed_when: False
      rescue:
        - name: Install emacs on Termux
          command: pkg install -y emacs
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure backports is added to sources.list.d
      ansible.builtin.apt_repository:
        repo: "deb http://deb.debian.org/debian {{ ansible_facts['distribution_release'] }}-backports main contrib non-free non-free-firmware"
        state: present
        update_cache: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Debian"
      become: yes
      ignore_errors: yes



    - name: Install emacs via debian on Debian
      block:
        - name: Check if emacs is installed
          shell: command -v emacs
          changed_when: False
      rescue:
        - name: Install emacs on non-Termux, non-MacOS systems
          package:
            name: emacs
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Debian"

    - name: Check if GPG key for emacs exists
      ansible.builtin.stat:
        path: /etc/apt/keyrings/ubuntuhandbook1-emacs-ppa.gpg
      register: emacs_gpg_key
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Ubuntu"

    - name: Download GPG key for emacs
      ansible.builtin.get_url:
        url: https://keyserver.ubuntu.com/pks/lookup?op=get&search=0xF4E48910A020E77056748B745738AE8480447DDF&options=mr
        dest: /tmp/emacs-repo.gpg
        mode: '0644'
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Ubuntu" and not emacs_gpg_key.stat.exists

    - name: Dearmor and install GPG key for emacs
      ansible.builtin.shell: |
        cat /tmp/emacs-repo.gpg | gpg --dearmor -o /etc/apt/keyrings/ubuntuhandbook1-emacs-ppa.gpg
      args:
        creates: /etc/apt/keyrings/ubuntuhandbook1-emacs-ppa.gpg
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Ubuntu" and not emacs_gpg_key.stat.exists

    - name: Add apt repository for emacs
      ansible.builtin.apt_repository:
        repo: "deb [signed-by=/etc/apt/keyrings/ubuntuhandbook1-emacs-ppa.gpg] https://ppa.launchpadcontent.net/ubuntuhandbook1/emacs/ubuntu {{ ansible_facts['distribution_release'] }} main"
        state: present
        filename: emacs
        update_cache: yes
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Ubuntu"



    - name: Install emacs via apt-repo on Ubuntu
      block:
        - name: Check if emacs is installed
          shell: command -v emacs
          changed_when: False
      rescue:
        - name: Install emacs via apt
          ansible.builtin.apt:
            name: emacs
            state: present
            update_cache: yes
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and ansible_facts['distribution'] == "Ubuntu"
