---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: uv.yml
  when: (ansible_facts['env']['TERMUX_VERSION'] is defined) or (ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin")

- name: Ensure mkdocs is present
  hosts: all
  tasks:
    - name: Include guard for mkdocs playbook
      block:
        - name: Stop early if the mkdocs playbook is already included
          meta: end_play
          when: mkdocs_playbook_imported is defined
        - name: Ensure the mkdocs playbook is not included
          set_fact:
            mkdocs_playbook_imported: true
          when: mkdocs_playbook_imported is not defined

    - name: Ensure mkdocs is present on MacOS
      block:
        - name: Check if mkdocs is installed
          shell: command -v mkdocs
          changed_when: False
      rescue:
        - name: Install mkdocs on MacOS
          community.general.homebrew:
            name: mkdocs-material
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure mkdocs is present on Termux
      block:
        - name: Ensure mkdocs is installed with uv
          shell: uv tool install mkdocs-material
          args:
            creates: ~/.local/bin/mkdocs
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Ensure mkdocs is installed with uv
      shell: uv tool install mkdocs-material
      args:
        creates: ~/.local/bin/mkdocs
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
