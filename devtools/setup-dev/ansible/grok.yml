---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: curl.yml

- name: Ensure grok is present
  hosts: all
  tasks:
    - name: Include guard for grok playbook
      block:
        - name: Stop early if the grok playbook is already included
          meta: end_play
          when: grok_playbook_imported is defined
        - name: Ensure the grok playbook is not included
          set_fact:
            grok_playbook_imported: true
          when: grok_playbook_imported is not defined

    - name: Ensure grok is present on MacOS
      block:
        - name: Check if grok is installed
          shell: command -v grok
          register: grok_installed
          failed_when: false
          changed_when: False

        - name: Install grok
          shell: |
            {{ playbook_dir }}/verified-run exec https://x.ai/cli/install.sh
          when: grok_installed.rc != 0
      when: ansible_facts['os_family'] == "Darwin"

    - name: Install grok via shell on Debian/Ubuntu
      block:
        - name: Check if grok is installed
          shell: command -v grok
          register: grok_installed
          failed_when: false
          changed_when: False

        - name: Install grok
          shell: |
            {{ playbook_dir }}/verified-run exec https://x.ai/cli/install.sh
          when: grok_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
