---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure swiftlint is present
  hosts: all
  tasks:
    - name: Include guard for swiftlint playbook
      block:
        - name: Stop early if the swiftlint playbook is already included
          meta: end_play
          when: swiftlint_playbook_imported is defined
        - name: Ensure the swiftlint playbook is not included
          set_fact:
            swiftlint_playbook_imported: true
          when: swiftlint_playbook_imported is not defined

    - name: Ensure swiftlint is present on MacOS
      block:
        - name: Check if swiftlint is installed
          shell: command -v swiftlint
          changed_when: False
      rescue:
        - name: Install swiftlint on MacOS
          community.general.homebrew:
            name: swiftlint
            state: present
      when: ansible_facts['os_family'] == "Darwin"
