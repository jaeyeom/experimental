---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure cloudflared is present
  hosts: all
  tasks:
    - name: Include guard for cloudflared playbook
      block:
        - name: Stop early if the cloudflared playbook is already included
          meta: end_play
          when: cloudflared_playbook_imported is defined
        - name: Ensure the cloudflared playbook is not included
          set_fact:
            cloudflared_playbook_imported: true
          when: cloudflared_playbook_imported is not defined

    - name: Ensure cloudflared is present on MacOS
      block:
        - name: Check if cloudflared is installed
          shell: command -v cloudflared
          changed_when: False
      rescue:
        - name: Install cloudflared on MacOS
          community.general.homebrew:
            name: cloudflared
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Check if GPG key for cloudflared exists
      ansible.builtin.stat:
        path: /usr/share/keyrings/cloudflare-main.gpg
      register: cloudflared_gpg_key
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Download GPG key for cloudflared
      ansible.builtin.get_url:
        url: https://pkg.cloudflare.com/cloudflare-main.gpg
        dest: /tmp/cloudflared-repo.gpg
        mode: '0644'
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not cloudflared_gpg_key.stat.exists

    - name: Dearmor and install GPG key for cloudflared
      ansible.builtin.shell: |
        cat /tmp/cloudflared-repo.gpg | gpg --dearmor -o /usr/share/keyrings/cloudflare-main.gpg
      args:
        creates: /usr/share/keyrings/cloudflare-main.gpg
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not cloudflared_gpg_key.stat.exists

    - name: Add apt repository for cloudflared
      ansible.builtin.apt_repository:
        repo: "deb [signed-by=/usr/share/keyrings/cloudflare-main.gpg] https://pkg.cloudflare.com/cloudflared any main"
        state: present
        filename: cloudflared
        update_cache: yes
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"



    - name: Install cloudflared via apt-repo on Debian/Ubuntu
      block:
        - name: Check if cloudflared is installed
          shell: command -v cloudflared
          changed_when: False
      rescue:
        - name: Install cloudflared via apt
          ansible.builtin.apt:
            name: cloudflared
            state: present
            update_cache: yes
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
