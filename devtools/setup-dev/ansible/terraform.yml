---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure terraform is present
  hosts: all
  tasks:
    - name: Include guard for terraform playbook
      block:
        - name: Stop early if the terraform playbook is already included
          meta: end_play
          when: terraform_playbook_imported is defined
        - name: Ensure the terraform playbook is not included
          set_fact:
            terraform_playbook_imported: true
          when: terraform_playbook_imported is not defined

    - name: Tap hashicorp/tap for terraform
      community.general.homebrew_tap:
        name: hashicorp/tap
        state: present
      when: ansible_facts['os_family'] == "Darwin"



    - name: Ensure terraform is present on MacOS
      block:
        - name: Check if terraform is installed
          shell: command -v terraform
          changed_when: False
      rescue:
        - name: Install terraform on MacOS
          community.general.homebrew:
            name: hashicorp/tap/terraform
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Check if GPG key for terraform exists
      ansible.builtin.stat:
        path: /usr/share/keyrings/hashicorp-archive-keyring.gpg
      register: terraform_gpg_key
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Download GPG key for terraform
      ansible.builtin.get_url:
        url: https://apt.releases.hashicorp.com/gpg
        dest: /tmp/terraform-repo.gpg
        mode: '0644'
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not terraform_gpg_key.stat.exists

    - name: Dearmor and install GPG key for terraform
      ansible.builtin.shell: |
        cat /tmp/terraform-repo.gpg | gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg
      args:
        creates: /usr/share/keyrings/hashicorp-archive-keyring.gpg
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin" and not terraform_gpg_key.stat.exists

    - name: Add apt repository for terraform
      ansible.builtin.apt_repository:
        repo: "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com {{ ansible_facts['distribution_release'] }} main"
        state: present
        filename: terraform
        update_cache: yes
      become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"



    - name: Install terraform via apt-repo on Debian/Ubuntu
      block:
        - name: Check if terraform is installed
          shell: command -v terraform
          changed_when: False
      rescue:
        - name: Install terraform via apt
          ansible.builtin.apt:
            name: terraform
            state: present
            update_cache: yes
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
