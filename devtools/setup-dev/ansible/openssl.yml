---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure openssl is present
  hosts: all
  tasks:
    - name: Include guard for openssl playbook
      block:
        - name: Stop early if the openssl playbook is already included
          meta: end_play
          when: openssl_playbook_imported is defined
        - name: Ensure the openssl playbook is not included
          set_fact:
            openssl_playbook_imported: true
          when: openssl_playbook_imported is not defined

    - name: Ensure openssl is present on MacOS
      block:
        - name: Check if openssl is installed
          shell: command -v openssl
          changed_when: False
      rescue:
        - name: Install openssl on MacOS
          community.general.homebrew:
            name: openssl
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure openssl is present on non-Termux, non-MacOS systems
      block:
        - name: Check if openssl is installed
          shell: command -v openssl
          changed_when: False
      rescue:
        - name: Install openssl on non-Termux, non-MacOS systems
          package:
            name: openssl
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure openssl is present on Termux
      block:
        - name: Check if openssl is installed
          shell: command -v openssl
          changed_when: False
      rescue:
        - name: Install openssl on Termux
          command: pkg install -y openssl-tool
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
