---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- import_playbook: gather-facts.yml
- import_playbook: setup-user-bin-directory.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined or ansible_facts['os_family'] == "Darwin"
- import_playbook: curl.yml
  when: ansible_facts['env']['TERMUX_VERSION'] is defined or ansible_facts['os_family'] == "Darwin"

- name: Ensure e is present
  hosts: all
  tasks:
    - name: Include guard for e playbook
      block:
        - name: Stop early if the e playbook is already included
          meta: end_play
          when: e_playbook_imported is defined
        - name: Ensure the e playbook is not included
          set_fact:
            e_playbook_imported: true
          when: e_playbook_imported is not defined

    - name: Ensure e is present on MacOS
      block:
        - name: Check if e is installed
          shell: command -v e
          register: e_installed
          failed_when: false
          changed_when: False

        - name: Install e
          shell: |
            curl -fsSL https://raw.githubusercontent.com/kilobyte/e/master/e -o {{ user_bin_directory }}/e && chmod 0700 {{ user_bin_directory }}/e
          when: e_installed.rc != 0
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure e is present on Termux
      block:
        - name: Check if e is installed
          shell: command -v e
          register: e_installed
          failed_when: false
          changed_when: False

        - name: Install e
          shell: |
            curl -fsSL https://raw.githubusercontent.com/kilobyte/e/master/e -o {{ user_bin_directory }}/e && chmod 0700 {{ user_bin_directory }}/e
          when: e_installed.rc != 0
      when: ansible_facts['env']['TERMUX_VERSION'] is defined

    - name: Install e via package on Debian/Ubuntu
      block:
        - name: Check if e is installed
          shell: command -v e
          changed_when: False
      rescue:
        - name: Install e on non-Termux, non-MacOS systems
          package:
            name: e-wrapper
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"
