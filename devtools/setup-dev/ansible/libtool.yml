---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure libtool is present
  hosts: all
  tasks:
    - name: Include guard for libtool playbook
      block:
        - name: Stop early if the libtool playbook is already included
          meta: end_play
          when: libtool_playbook_imported is defined
        - name: Ensure the libtool playbook is not included
          set_fact:
            libtool_playbook_imported: true
          when: libtool_playbook_imported is not defined

    - name: Ensure libtool is present on MacOS
      block:
        - name: Check if libtool is installed
          shell: command -v libtool
          changed_when: False
      rescue:
        - name: Install libtool on MacOS
          community.general.homebrew:
            name: libtool
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure libtool is present on non-Termux, non-MacOS systems
      block:
        - name: Check if libtool is installed
          shell: command -v libtool
          changed_when: False
      rescue:
        - name: Install libtool on non-Termux, non-MacOS systems
          package:
            name: libtool-bin
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure libtool is present on Termux
      block:
        - name: Check if libtool is installed
          shell: command -v libtool
          changed_when: False
      rescue:
        - name: Install libtool on Termux
          command: pkg install -y libtool
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
