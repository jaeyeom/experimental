---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make
- name: Ensure make is present
  hosts: all
  tasks:
    - name: Include guard for make playbook
      block:
        - name: Stop early if the make playbook is already included
          meta: end_play
          when: make_playbook_imported is defined
        - name: Ensure the make playbook is not included
          set_fact:
            make_playbook_imported: true
          when: make_playbook_imported is not defined

    - name: Ensure make is present on MacOS
      block:
        - name: Check if make is installed
          shell: command -v make
          changed_when: False
      rescue:
        - name: Install make on MacOS
          community.general.homebrew:
            name: make
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Ensure make is present on non-Termux, non-MacOS systems
      block:
        - name: Check if make is installed
          shell: command -v make
          changed_when: False
      rescue:
        - name: Install make on non-Termux, non-MacOS systems
          package:
            name: make
            state: present
          become: yes
      when: ansible_facts['env']['TERMUX_VERSION'] is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Ensure make is present on Termux
      block:
        - name: Check if make is installed
          shell: command -v make
          changed_when: False
      rescue:
        - name: Install make on Termux
          command: pkg install -y make
      when: ansible_facts['env']['TERMUX_VERSION'] is defined
