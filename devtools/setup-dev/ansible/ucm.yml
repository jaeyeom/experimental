---
# THIS FILE IS AUTO-GENERATED by generate_packages.go - DO NOT EDIT MANUALLY
# To make changes, modify generate_packages.go, install_methods.go, packages_data.go, or templates.go
# and then run: make

- name: Ensure ucm is present
  hosts: all
  tasks:
    - name: Include guard for ucm playbook
      block:
        - name: Stop early if the ucm playbook is already included
          meta: end_play
          when: ucm_playbook_imported is defined
        - name: Ensure the ucm playbook is not included
          set_fact:
            ucm_playbook_imported: true
          when: ucm_playbook_imported is not defined

    - name: Tap unisonweb/unison for ucm
      community.general.homebrew_tap:
        name: unisonweb/unison
        state: present
      when: ansible_facts['os_family'] == "Darwin"



    - name: Ensure ucm is present on MacOS
      block:
        - name: Check if ucm is installed
          shell: command -v ucm
          changed_when: False
      rescue:
        - name: Install ucm on MacOS
          community.general.homebrew:
            name: unison-language
            state: present
      when: ansible_facts['os_family'] == "Darwin"

    - name: Check if GPG key for ucm exists
      ansible.builtin.stat:
        path: /etc/apt/trusted.gpg.d/unison-computing.gpg
      register: ucm_gpg_key
      when: ansible_env.TERMUX_VERSION is not defined and ansible_facts['os_family'] != "Darwin"

    - name: Download GPG key for ucm
      ansible.builtin.get_url:
        url: https://debian.unison-lang.org/public.gpg
        dest: /tmp/ucm-repo.gpg
        mode: '0644'
      become: yes
      when: ansible_env.TERMUX_VERSION is not defined and ansible_facts['os_family'] != "Darwin" and not ucm_gpg_key.stat.exists

    - name: Dearmor and install GPG key for ucm
      ansible.builtin.shell: |
        cat /tmp/ucm-repo.gpg | gpg --dearmor -o /etc/apt/trusted.gpg.d/unison-computing.gpg
      args:
        creates: /etc/apt/trusted.gpg.d/unison-computing.gpg
      become: yes
      when: ansible_env.TERMUX_VERSION is not defined and ansible_facts['os_family'] != "Darwin" and not ucm_gpg_key.stat.exists

    - name: Add apt repository for ucm
      ansible.builtin.apt_repository:
        repo: "deb [arch=amd64 signed-by=/etc/apt/trusted.gpg.d/unison-computing.gpg] https://debian.unison-lang.org/ trixie main"
        state: present
        filename: ucm
        update_cache: yes
      become: yes
      when: ansible_env.TERMUX_VERSION is not defined and ansible_facts['os_family'] != "Darwin"



    - name: Install ucm via apt-repo on Debian/Ubuntu
      block:
        - name: Check if ucm is installed
          shell: command -v ucm
          changed_when: False
      rescue:
        - name: Install ucm via apt
          ansible.builtin.apt:
            name: unisonweb
            state: present
            update_cache: yes
          become: yes
      when: ansible_env.TERMUX_VERSION is not defined and ansible_facts['os_family'] != "Darwin"
